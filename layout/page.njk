{% extends 'includes/layout.njk' %}

{% block body %}
    <article id="page">
        <h1>{{ page.title }}</h1>
        <div class="content">
            {{ page.content }}
        </div>
        {{ page.date }}
        {{ post_template.render(page) }}

        {{ page.link }}
    </article>

    {% include 'includes/pagination.njk' %}
{% endblock %}
